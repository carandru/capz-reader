const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const esc=(x='')=>String(x).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const api=async(url,opt={})=>{opt.headers={...(opt.headers||{}),'X-Requested-With':'NASPDFReader'};if(opt.body&&typeof opt.body!=='string'){opt.headers['Content-Type']='application/json';opt.body=JSON.stringify(opt.body)}const r=await fetch(url,opt);if(r.status===401){showAuth(false);throw new Error('Login required')}if(!r.ok){let x;try{x=await r.json()}catch{x={detail:await r.text()}}throw new Error(typeof x.detail==='string'?x.detail:JSON.stringify(x.detail||x))}return r.json()};
let state={books:[],allSeries:[],selected:new Set(),selection:false,category:localStorage.getItem('nasreader.category')||'',q:'',series:'',sort:localStorage.getItem('nasreader.sort')||'title',view:localStorage.getItem('nasreader.view')||'books',allowDelete:false,longPress:null,lastSelected:null,suppressClick:null,counts:{}};
const toast=m=>{const t=$('#toast');t.textContent=m;t.classList.remove('hidden');setTimeout(()=>t.classList.add('hidden'),2200)};

async function authInit(){const s=await fetch('/api/auth/status').then(r=>r.json());if(s.authenticated){$('#auth').classList.add('hidden');$('#app').classList.remove('hidden');restoreControls();load()}else showAuth(s.setup_required)}
function showAuth(setup){$('#app').classList.add('hidden');$('#auth').classList.remove('hidden');$('#authHint').textContent=setup?'First run: create the only admin account.':'Login to your private library.';$('#authBtn').textContent=setup?'Create account':'Login';$('#authBtn').onclick=async()=>{try{$('#authError').textContent='';await api(setup?'/api/auth/setup':'/api/auth/login',{method:'POST',body:{username:$('#username').value,password:$('#password').value}});$('#auth').classList.add('hidden');$('#app').classList.remove('hidden');restoreControls();load()}catch(e){$('#authError').textContent=e.message}}}
function restoreControls(){ $$('#categoryTabs button').forEach(b=>b.classList.toggle('active',b.dataset.category===state.category));$('#sort').value=state.sort;syncViewButtons() }
function query(){const p=new URLSearchParams();if(state.q)p.set('q',state.q);if(state.category)p.set('category',state.category);if(state.series)p.set('series',state.series);p.set('sort',state.sort);if($('#showArchived').checked)p.set('show_archived','true');if($('#favorites').checked)p.set('favorites','true');return p.toString()}
async function load(){const d=await api('/api/library?'+query());state.books=d.books;state.allSeries=d.series;state.allowDelete=d.allow_delete_files;state.counts=d.category_counts||{};$('#physicalDelete').classList.toggle('hidden',!state.allowDelete);$('#countBadge').textContent=`${d.count} books`;renderCategoryCounts(d.total_count||d.count);renderSeriesFilter(d.series);renderContinue();render();renderScan(d.scan)}
function renderCategoryCounts(total){ $$('[data-count]').forEach(x=>{const k=x.dataset.count,n=k==='All'?total:(state.counts[k]||0);x.textContent=`${n}`}) }
function renderSeriesFilter(list){const s=$('#seriesFilter'),old=state.series,filtered=list.filter(x=>!state.category||x.category===state.category);s.innerHTML='<option value="">All series</option>'+filtered.map(x=>`<option value="${esc(x.series)}">${esc(x.series)} (${x.count})</option>`).join('');if([...s.options].some(o=>o.value===old))s.value=old;else{state.series='';s.value=''}}
function renderContinue(){const books=state.books.filter(b=>b.read_state==='reading' || (b.last_page>1&&b.read_state!=='read')).sort((a,b)=>(b.last_opened||'').localeCompare(a.last_opened||'')).slice(0,12);const sec=$('#continueSection');if(!books.length||state.selection){sec.classList.add('hidden');return}sec.classList.remove('hidden');$('#continueRow').innerHTML=books.map(b=>`<button class="continue-card" data-open-reader="${b.id}"><img src="/api/books/${b.id}/thumb" loading="lazy"><span><strong>${esc(b.title)}</strong><small>${b.last_page}/${b.page_count} · ${b.progress}%</small></span></button>`).join('');$$('[data-open-reader]').forEach(x=>x.onclick=()=>location.href=`/reader/${x.dataset.openReader}`)}
function syncViewButtons(){ $$('#viewToggle button').forEach(b=>b.classList.toggle('active',b.dataset.view===state.view)); }
function setView(v){state.view=v;localStorage.setItem('nasreader.view',v);syncViewButtons();render()}
function render(){const g=$('#grid');g.classList.toggle('select-mode',state.selection);$('#seriesBack').classList.toggle('hidden',!state.series);$('#seriesTitle').textContent=state.series||'';if(state.series||state.selection||state.view==='books')renderBooks(g);else renderSeriesCards(g);$('#empty').classList.toggle('hidden',state.books.length>0);updateBulk()}
function bookCard(b){return `<article class="book ${state.selected.has(b.id)?'selected':''}" data-id="${b.id}"><div class="cover-wrap"><img class="cover" loading="lazy" src="/api/books/${b.id}/thumb"><div class="select-dot">${state.selected.has(b.id)?'✓':''}</div><button class="star" data-star="${b.id}" aria-label="Favorite">${b.favorite?'★':'☆'}</button><div class="progress"><i style="width:${b.progress}%"></i></div></div><div class="book-info"><div class="book-title">${esc(b.title)}</div><div class="meta"><span>${esc(b.series||b.category)}</span><span>${b.page_count}p</span></div><div class="chips">${b.volume?`<span class="chip">#${esc(b.volume)}</span>`:''}${b.tags.slice(0,2).map(t=>`<span class="chip">${esc(t)}</span>`).join('')}</div></div></article>`}
function renderBooks(g){g.classList.remove('series-grid');g.innerHTML=state.books.map(bookCard).join('');bindCards()}
function renderSeriesCards(g){const grouped=new Map(),standalone=[];for(const b of state.books){if(!b.series){standalone.push(b);continue}const key=`${b.category}\u0000${b.series}`;if(!grouped.has(key))grouped.set(key,[]);grouped.get(key).push(b)}const cards=[...grouped.values()].sort((a,b)=>natural(a[0].series).localeCompare(natural(b[0].series),undefined,{numeric:true,sensitivity:'base'})).map(items=>{items.sort((a,b)=>natural(a.volume||a.title).localeCompare(natural(b.volume||b.title),undefined,{numeric:true,sensitivity:'base'}));const first=items[0],read=items.filter(x=>x.read_state==='read').length,avg=Math.round(items.reduce((s,x)=>s+x.progress,0)/items.length);return `<article class="series-card" data-series="${esc(first.series)}" data-category="${esc(first.category)}"><div class="series-covers"><img src="/api/books/${first.id}/thumb" loading="lazy"></div><div class="series-info"><div class="book-title">${esc(first.series)}</div><div class="meta"><span>${items.length} books</span><span>${read}/${items.length} read</span></div><div class="progress"><i style="width:${avg}%"></i></div><small>Long-press to select the whole series</small></div></article>`}).join('');g.classList.add('series-grid');g.innerHTML=cards+(standalone.length?`<div class="standalone-label">Standalone</div>${standalone.map(bookCard).join('')}`:'');bindSeriesCards();bindCards()}
function natural(x=''){return String(x).toLowerCase()}
function bindSeriesCards(){ $$('.series-card').forEach(card=>{let lp=null,suppress=false;const name=card.dataset.series;card.addEventListener('pointerdown',e=>{if(e.pointerType==='touch')lp=setTimeout(()=>{suppress=true;state.view='books';setSelection(true);state.books.filter(b=>b.series===name).forEach(b=>state.selected.add(b.id));navigator.vibrate?.(20);render()},450)});['pointerup','pointercancel','pointermove'].forEach(ev=>card.addEventListener(ev,()=>clearTimeout(lp)));card.onclick=()=>{if(suppress){suppress=false;return}state.series=name;$('#seriesFilter').value=name;state.view='books';syncViewButtons();load()}})}
function bindCards(){ $$('.book').forEach(card=>{const id=+card.dataset.id;card.addEventListener('pointerdown',e=>{if(e.pointerType==='touch'&&!state.selection)state.longPress=setTimeout(()=>{state.suppressClick=id;state.view='books';setSelection(true);toggle(id);navigator.vibrate?.(20)},450)});['pointerup','pointercancel','pointermove'].forEach(ev=>card.addEventListener(ev,()=>clearTimeout(state.longPress)));card.addEventListener('click',e=>{if(state.suppressClick===id){state.suppressClick=null;return}if(e.target.closest('.star'))return;if(state.selection){if(e.shiftKey&&state.lastSelected){const ids=state.books.map(x=>x.id),a=ids.indexOf(state.lastSelected),b=ids.indexOf(id);ids.slice(Math.min(a,b),Math.max(a,b)+1).forEach(x=>state.selected.add(x));render()}else toggle(id);state.lastSelected=id}else openDetails(id)})});$$('[data-star]').forEach(x=>x.onclick=async e=>{e.stopPropagation();const id=+x.dataset.star,b=state.books.find(z=>z.id===id);await api(`/api/books/${id}`,{method:'PATCH',body:{favorite:!b.favorite}});load()}) }
function setSelection(v){state.selection=v;if(v)state.view='books';if(!v)state.selected.clear();$('#selectBtn').textContent=v?'Done':'Select';renderContinue();render()}
function toggle(id){state.selected.has(id)?state.selected.delete(id):state.selected.add(id);render()}
function updateBulk(){$('#bulkBar').classList.toggle('hidden',!state.selection);$('#selectedCount').textContent=`${state.selected.size} selected`}

async function openDetails(id){const b=await api(`/api/books/${id}`);const pages=[1,Math.min(2,b.page_count),Math.min(3,b.page_count)].filter((x,i,a)=>x&&a.indexOf(x)===i);$('#modalBody').innerHTML=`<h2>${esc(b.title)}</h2><div class="modal-grid">${pages.map(p=>`<img loading="lazy" src="/api/books/${id}/preview/${p}">`).join('')}</div><div class="form-grid" style="margin-top:14px"><label>Title<input id="eTitle" value="${esc(b.title)}"></label><label>Category<select id="eCategory"><option ${b.category==='Novel'?'selected':''}>Novel</option><option ${b.category==='Manga'?'selected':''}>Manga</option><option ${b.category==='Dou'?'selected':''}>Dou</option></select></label><label>Series<input id="eSeries" value="${esc(b.series||'')}"></label><label>Volume / Part<input id="eVolume" value="${esc(b.volume||'')}"></label><label>Cover page<input id="eCover" type="number" min="1" max="${b.page_count}" value="${b.cover_page}"></label><label>Tags<input id="eTags" value="${esc(b.tags.join(', '))}"></label><div class="row"><a class="button-link primary" href="/reader/${id}">Read from page ${b.last_page}</a><a class="button-link" href="/api/books/${id}/file?download=true">Download PDF</a><button id="saveEdit" class="primary">Save</button></div><div class="meta path-meta"><span>${esc(b.rel_path)}</span><span>${b.progress}%</span></div></div>`;showModal();$('#saveEdit').onclick=async()=>{await api(`/api/books/${id}`,{method:'PATCH',body:{title:$('#eTitle').value,category:$('#eCategory').value,series:$('#eSeries').value||null,volume:$('#eVolume').value||null,cover_page:+$('#eCover').value,tags:$('#eTags').value.split(',').map(x=>x.trim()).filter(Boolean)}});hideModal();toast('Saved');load()}}
function showModal(){$('#modal').classList.remove('hidden')}function hideModal(){$('#modal').classList.add('hidden')}
function actionSheet(title,html,onApply){$('#modalBody').innerHTML=`<h2>${esc(title)}</h2>${html}<div class="row modal-actions"><button id="actionCancel">Cancel</button><button id="actionApply" class="primary">Apply</button></div>`;showModal();$('#actionCancel').onclick=hideModal;$('#actionApply').onclick=onApply}
async function runBulk(action,value){const ids=[...state.selected];if(!ids.length)return toast('Select books first');await api('/api/bulk',{method:'POST',body:{ids,action,value}});toast(`Updated ${ids.length} books`);hideModal();state.selected.clear();setSelection(false);load()}
function bulkPrompt(kind){const n=state.selected.size;if(!n)return toast('Select books first');if(kind==='category')return actionSheet(`Move ${n} books to category`,`<div class="choice-grid">${['Novel','Manga','Dou'].map(x=>`<button class="choice" data-value="${x}">${x}</button>`).join('')}</div>`,()=>{const v=$('.choice.selected')?.dataset.value;if(!v)return toast('Choose a category');runBulk('category',v)}),bindChoiceSoon();if(kind==='series'){const opts=[...new Set(state.allSeries.filter(x=>!state.category||x.category===state.category).map(x=>x.series))].sort();actionSheet(`Move ${n} books to series`,`<label>Series<input id="bulkSeries" list="seriesNames" placeholder="Blank = Standalone"></label><datalist id="seriesNames">${opts.map(x=>`<option value="${esc(x)}">`).join('')}</datalist>`,()=>runBulk('series',$('#bulkSeries').value));return}if(kind==='tag'){actionSheet(`Tags for ${n} books`,`<label>Tag<input id="bulkTag" placeholder="Tag name"></label><div class="choice-grid"><button class="choice selected" data-value="add">Add</button><button class="choice" data-value="remove">Remove</button></div>`,()=>{const t=$('#bulkTag').value.trim();if(!t)return toast('Enter a tag');runBulk($('.choice.selected')?.dataset.value==='remove'?'remove_tag':'add_tag',t)});bindChoiceSoon();return}if(kind==='read'){actionSheet(`Read state for ${n} books`,`<div class="choice-grid">${['unread','reading','read'].map(x=>`<button class="choice" data-value="${x}">${x}</button>`).join('')}</div>`,()=>{const v=$('.choice.selected')?.dataset.value;if(!v)return toast('Choose a state');runBulk('read_state',v)});bindChoiceSoon();return}if(kind==='favorite'){actionSheet(`Favorite ${n} books`,`<div class="choice-grid"><button class="choice selected" data-value="1">Favorite</button><button class="choice" data-value="0">Unfavorite</button></div>`,()=>runBulk('favorite',$('.choice.selected')?.dataset.value==='1'));bindChoiceSoon();return}if(kind==='archive'){actionSheet(`Archive ${n} books`,`<div class="choice-grid"><button class="choice selected" data-value="1">Archive</button><button class="choice" data-value="0">Unarchive</button></div>`,()=>runBulk('archive',$('.choice.selected')?.dataset.value==='1'));bindChoiceSoon();return}if(kind==='preview')return actionSheet(`Regenerate previews`,`<p>Clear cached covers/previews for ${n} selected books and regenerate covers in the background.</p>`,()=>runBulk('regen_preview',true));if(kind==='remove')return actionSheet(`Remove from library`,`<div class="warning-box">Remove ${n} books from the NAS PDF Reader index? <strong>PDF files stay on the NAS.</strong> They will be ignored by future scans until manually restored in a future version.</div>`,()=>runBulk('remove_library',true));if(kind==='delete')return actionSheet(`Delete PDF files`,`<div class="danger-box"><strong>Permanent deletion.</strong><br>Delete ${n} selected PDF files from the NAS? This cannot be undone.</div><label class="confirm-line"><input id="deleteConfirm" type="checkbox"> I understand these files will be deleted from the NAS.</label>`,()=>{if(!$('#deleteConfirm').checked)return toast('Confirm permanent deletion first');runBulk('delete_files',true)})}
function bindChoiceSoon(){setTimeout(()=>$$('.choice').forEach(b=>b.onclick=()=>{b.parentElement.querySelectorAll('.choice').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')}),0)}

async function renderScan(s){if(!s)return;const el=$('#scanStatus');if(s.running){el.classList.remove('hidden');el.textContent=`Scanning NAS… seen ${s.seen}, +${s.added}, updated ${s.updated}`;setTimeout(async()=>renderScan(await api('/api/scan/status')),1000)}else if(s.error){el.classList.remove('hidden');el.textContent='Scan error: '+s.error}else el.classList.add('hidden')}
$('#scanBtn').onclick=async()=>renderScan((await api('/api/scan',{method:'POST'})).scan);
$('#selectBtn').onclick=()=>setSelection(!state.selection);$('#bulkClose').onclick=()=>setSelection(false);$('#selectVisible').onclick=()=>{state.books.forEach(b=>state.selected.add(b.id));render()};$$('[data-bulk]').forEach(b=>b.onclick=()=>bulkPrompt(b.dataset.bulk));$('#modalX').onclick=hideModal;$('#modal').onclick=e=>{if(e.target===$('#modal'))hideModal()};$('#logoutBtn').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});location.reload()};
$('#search').oninput=debounce(()=>{state.q=$('#search').value;load()},250);$('#seriesFilter').onchange=()=>{state.series=$('#seriesFilter').value;if(state.series)state.view='books';load()};$('#sort').onchange=()=>{state.sort=$('#sort').value;localStorage.setItem('nasreader.sort',state.sort);load()};$('#favorites').onchange=load;$('#showArchived').onchange=load;$('#seriesBackBtn').onclick=()=>{state.series='';$('#seriesFilter').value='';state.view='series';syncViewButtons();load()};
$$('#categoryTabs button').forEach(b=>b.onclick=()=>{$$('#categoryTabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');state.category=b.dataset.category;localStorage.setItem('nasreader.category',state.category);state.series='';load()});$$('#viewToggle button').forEach(b=>b.onclick=()=>setView(b.dataset.view));
function debounce(fn,ms){let t;return()=>{clearTimeout(t);t=setTimeout(fn,ms)}}
if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});authInit();
